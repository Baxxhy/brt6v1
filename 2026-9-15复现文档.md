# TRAIT 2026-09-15 空服务器复现文档

本文用于在一台全新的 Ubuntu 服务器上，从 Git 仓库开始复现 TRAIT 在 SWT-Bench Lite 276 个实例上的完整实验。服务器可以没有 Git、Conda、Python 环境、Docker、benchmark 源码、Docker 镜像和官方评测数据。

本文对应的冻结实验入口是：

```text
scripts/run_trait_xiaojing_deepseekv4flash_full.sh
```

完整方法依次执行：

```text
Issue + 检索源码 + 检索测试
→ 组件 1：恢复一个明确的 Reproduction Target
→ 组件 2：分别适配三个 retrieved tests，并用执行反馈更新 Semantic Delta
→ 严格语义验证器保留 ISSUE_ALIGNED_FAIL 候选
→ 本地 iCoRe 确定性排序
→ 每个实例输出一个 BRT
→ 官方 SWT-Docker F2P 评测
```

当前正式流程不启用行为歧义分支，也不调用后置 Issue2Test LLM judge。组件 2 生成候选时已经使用严格语义验证器判断 buggy failure 是否与 issue 对齐，后续只读取冻结结果并执行本地排序。

本次提交只有一个论文正式入口：`scripts/run_trait_xiaojing_deepseekv4flash_full.sh`。仓库中以 `run_p0_`、`component2_`、`rollback`、`surrogate` 或旧日期命名的脚本和文档仅用于历史实验或诊断，不应替代该入口。正式入口的 `run_config.json` 是实际运行配置的审计依据。

## 1. 冻结配置

| 配置 | 固定值 |
|---|---|
| 数据集 | SWT-Bench Lite 子集，共 276 条 |
| 模型服务 | `https://api.open.xiaojingai.com/v1` |
| 模型 | `deepseek-v4-flash` |
| 推理模式 | 关闭 |
| 提示词 | 英文精简版 |
| 温度 | `0.1` |
| 初始最大输出 | 4096 tokens |
| 截断重试最大输出 | 8192 tokens |
| 组件 1 并发 | 20 |
| 组件 2 并发 | 20 |
| 最终排序并发 | 20 |
| 官方评测并发 | 20 |
| API 请求超时 | 1200 秒 |
| 单实例 Docker 超时 | 7200 秒 |
| Semantic Delta 最大轮数 | 5 |
| Coverage | 关闭，只评测 F2P |

精简提示词只删除重复的原始 BehaviorTarget 副本和冗余协议字段。Issue、结构化复现目标、检索测试、Semantic Delta、候选代码和执行反馈仍然保留。三例实测中，生成、Delta 和严格验证提示词平均缩短约 30%～46%。

## 2. 服务器要求

本文假设：

- Ubuntu 22.04 或 24.04，Linux x86-64；
- 使用 `root` 用户；
- 能访问 GitHub、Docker Hub、Ubuntu 软件源、conda-forge、PyPI、Hugging Face 和模型 API；
- 不需要 GPU；
- 建议至少 16 核 CPU、64 GiB 内存和 300 GiB 可用 SSD；
- Docker 数据目录在实验运行期间应保持至少 120 GiB 可用空间。

本项目固定使用以下路径：

```text
/root/Baxxhy/BugReproduce/brt6
/root/Baxxhy/BugReproduce/swe_repos
/root/miniconda3/envs/icore
/root/miniconda3/envs/swtbench
/root/lby-docker
/run/mutate-docker.sock
```

## 3. 安装基础软件

```bash
sudo -i
apt-get update
DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
  bash build-essential ca-certificates curl git jq locales patch pkg-config \
  procps rsync tmux unzip util-linux xz-utils
sed -i 's/^# *en_US.UTF-8 UTF-8/en_US.UTF-8 UTF-8/' /etc/locale.gen
locale-gen en_US.UTF-8
export LANG=en_US.UTF-8
export LC_ALL=en_US.UTF-8
```

## 4. 克隆发布仓库

将下面地址替换为实际远程仓库地址。目录名必须保持为 `brt6`。

```bash
mkdir -p /root/Baxxhy/BugReproduce
cd /root/Baxxhy/BugReproduce
git clone https://github.com/YOUR_ACCOUNT/YOUR_REPOSITORY.git brt6
cd /root/Baxxhy/BugReproduce/brt6
git rev-parse HEAD
```

检查运行必需的公开输入和代码：

```bash
for file in \
  requirements-reproduce.txt \
  data/issues/swt276_issues.json \
  retrieval_results/code/code_retrieval_results_gpt.json \
  retrieval_results/test/icore/gpt/related_tests.json \
  evaluation/vendor/swtbench/src/main.py \
  scripts/bootstrap_repositories.py \
  scripts/prepare_swt276_official_data.py \
  scripts/prepare_swt_images.py \
  scripts/run_generation_strict_icore.py \
  scripts/run_trait_xiaojing_deepseekv4flash_full.sh; do
  test -f "$file" || { echo "缺少文件：$file"; exit 1; }
done
```

## 5. 安装 Conda 和 Python 环境

```bash
mkdir -p /root/brt6-bootstrap
cd /root/brt6-bootstrap
curl -fL --retry 5 -o Miniforge3-Linux-x86_64.sh \
  https://github.com/conda-forge/miniforge/releases/latest/download/Miniforge3-Linux-x86_64.sh
bash Miniforge3-Linux-x86_64.sh -b -p /root/miniconda3
source /root/miniconda3/etc/profile.d/conda.sh
conda create -y -n icore python=3.12 pip
conda create -y -n swtbench python=3.12 pip
```

安装控制器和官方 harness 的宿主机依赖：

```bash
cd /root/Baxxhy/BugReproduce/brt6
/root/miniconda3/envs/icore/bin/python -m pip install -r requirements-reproduce.txt
/root/miniconda3/envs/swtbench/bin/python -m pip install -r requirements-reproduce.txt
/root/miniconda3/envs/icore/bin/python -m pip check
/root/miniconda3/envs/swtbench/bin/python -m pip check
```

benchmark 项目本身的依赖安装在官方 Docker 镜像中，不安装到这两个宿主机环境。

## 6. 安装并配置 Docker

安装 Docker Engine：

```bash
install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg \
  -o /etc/apt/keyrings/docker.asc
chmod a+r /etc/apt/keyrings/docker.asc
cat >/etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://download.docker.com/linux/ubuntu
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF
apt-get update
apt-get install -y docker-ce docker-ce-cli containerd.io \
  docker-buildx-plugin docker-compose-plugin
```

配置实验使用的独立 Docker daemon：

```bash
systemctl disable --now docker.service docker.socket || true
mkdir -p /root/lby-docker /etc/docker
cat >/etc/docker/brt6-daemon.json <<'EOF'
{
  "storage-driver": "overlay2",
  "features": {"containerd-snapshotter": false}
}
EOF
cat >/etc/systemd/system/mutate-docker.service <<'EOF'
[Unit]
Description=TRAIT Docker daemon
After=network-online.target containerd.service
Wants=network-online.target
Requires=containerd.service

[Service]
Type=notify
ExecStart=/usr/bin/dockerd --config-file=/etc/docker/brt6-daemon.json --host=unix:///run/mutate-docker.sock --data-root=/root/lby-docker --exec-root=/run/mutate-docker-exec --pidfile=/run/mutate-docker.pid --containerd=/run/containerd/containerd.sock
ExecReload=/bin/kill -s HUP $MAINPID
Restart=always
RestartSec=2
TimeoutStartSec=0
LimitNOFILE=1048576
TasksMax=infinity
Delegate=yes
KillMode=process

[Install]
WantedBy=multi-user.target
EOF
systemctl daemon-reload
systemctl enable --now containerd.service mutate-docker.service
export DOCKER_HOST=unix:///run/mutate-docker.sock
docker info --format 'driver={{.Driver}} root={{.DockerRootDir}}'
docker run --rm hello-world
df -h /root/lby-docker
```

预期 Docker 输出 `driver=overlay2 root=/root/lby-docker`。

## 7. 配置模型 API

真实 key 只写入本机忽略文件，不需要修改源码。配置程序使用隐藏输入，不在终端显示 key：

```bash
cd /root/Baxxhy/BugReproduce/brt6
/root/miniconda3/envs/icore/bin/python scripts/configure_api_keys.py \
  --output .secrets/api_pool_xiaojing_deepseekv4flash.json \
  --provider deepseek \
  --base-url https://api.open.xiaojingai.com/v1 \
  --model deepseek-v4-flash
```

交互时只需粘贴 key；地址和模型已经由命令固定。

检查权限和配置：

```bash
/root/miniconda3/envs/icore/bin/python scripts/configure_api_keys.py \
  --output .secrets/api_pool_xiaojing_deepseekv4flash.json --provider deepseek --check
stat -c '%a %n' .secrets/api_pool_xiaojing_deepseekv4flash.json
git check-ignore .secrets/api_pool_xiaojing_deepseekv4flash.json
```

预期权限为 `600`，`git check-ignore` 应输出该文件。测试真实请求：

```bash
export PYTHONPATH=/root/Baxxhy/BugReproduce
export BRT_API_POOL_FILE=/root/Baxxhy/BugReproduce/brt6/.secrets/api_pool_xiaojing_deepseekv4flash.json
export BRT_DISABLE_THINKING=1
export BRT_ALLOWED_API_HOST=api.open.xiaojingai.com
/root/miniconda3/envs/icore/bin/python scripts/check_configured_api.py \
  --provider deepseek --model deepseek-v4-flash --stream
```

预期输出：

```text
API request succeeded; response='OK'
```

正式入口同时固定配置文件、网关和模型。`BRT_ALLOWED_API_HOST` 会在请求前拒绝任何非 `api.open.xiaojingai.com` 的地址；命令行指定的 `deepseek-v4-flash` 也不会被 API 池中的模型别名覆盖。不要把 CSU 的通用 `.secrets/api_pool.json` 传给本入口。若配置错误，程序应立即报告 `configured LLM host ... is not allowed`，此时先修正本地专用配置文件，不要取消网关校验。

## 8. 准备数据集

仓库应当包含不带真实修复的生成输入：

```text
data/issues/swt276_issues.json
retrieval_results/code/code_retrieval_results_gpt.json
retrieval_results/test/icore/gpt/related_tests.json
```

官方评测文件包含公开的真实 patch，只在预测冻结后的官方评测阶段读取。它不提交到 Git，空服务器从公开 SWE-bench Lite 下载：

```bash
cd /root/Baxxhy/BugReproduce/brt6
/root/miniconda3/envs/icore/bin/python scripts/prepare_swt276_official_data.py
/root/miniconda3/envs/icore/bin/python scripts/prepare_swt276_official_data.py --check
```

校验生成输入和检索输入：

```bash
/root/miniconda3/envs/icore/bin/python - <<'PY'
import json
from pathlib import Path

root = Path('/root/Baxxhy/BugReproduce/brt6')
issues = json.loads((root / 'data/issues/swt276_issues.json').read_text())
ids = [row['instance_id'] for row in issues]
assert len(ids) == len(set(ids)) == 276
for relative in (
    'retrieval_results/code/code_retrieval_results_gpt.json',
    'retrieval_results/test/icore/gpt/related_tests.json',
):
    payload = json.loads((root / relative).read_text())
    missing = [instance_id for instance_id in ids if not payload.get(instance_id)]
    assert not missing, (relative, missing)
print('276 条生成输入和检索结果校验通过')
PY
```

## 9. 准备 benchmark 源码

```bash
mkdir -p /root/Baxxhy/BugReproduce/swe_repos
export PYTHONPATH=/root/Baxxhy/BugReproduce
/root/miniconda3/envs/icore/bin/python scripts/bootstrap_repositories.py \
  --dataset data/issues/swt276_issues.json \
  --repo-root /root/Baxxhy/BugReproduce/swe_repos \
  --manifest /root/Baxxhy/BugReproduce/brt6/repository_manifest.json
/root/miniconda3/envs/icore/bin/python scripts/bootstrap_repositories.py \
  --dataset data/issues/swt276_issues.json \
  --repo-root /root/Baxxhy/BugReproduce/swe_repos \
  --check-only
```

不要使用 shallow clone；所有实例对应的历史 commit 必须存在。

## 10. 构建官方 SWT-Docker 镜像

空服务器没有 `exec.eval.*` 镜像，必须先构建。镜像构建并发建议从 2 开始，避免第一次安装大量旧依赖时耗尽磁盘或内存。它不属于论文算法的 20 并发配置。

```bash
export PYTHONPATH=/root/Baxxhy/BugReproduce:/root/Baxxhy/BugReproduce/brt6/evaluation/vendor/swtbench:/root/Baxxhy/BugReproduce/brt6/evaluation/vendor/swtbench/src
export DOCKER_HOST=unix:///run/mutate-docker.sock
/root/miniconda3/envs/swtbench/bin/python scripts/prepare_swt_images.py \
  --dataset data/issues/swt276_issues.json \
  --repo-root /root/Baxxhy/BugReproduce/swe_repos \
  --workers 2
/root/miniconda3/envs/swtbench/bin/python scripts/prepare_swt_images.py \
  --dataset data/issues/swt276_issues.json \
  --repo-root /root/Baxxhy/BugReproduce/swe_repos \
  --check-only
```

最终必须输出：

```text
required=276 missing=0
```

## 11. 运行前检查

```bash
cd /root/Baxxhy/BugReproduce/brt6
export PYTHONPATH=/root/Baxxhy/BugReproduce
export DOCKER_HOST=unix:///run/mutate-docker.sock
bash -n scripts/run_trait_xiaojing_deepseekv4flash_full.sh
/root/miniconda3/envs/icore/bin/python -m py_compile \
  scripts/run_generation_strict_icore.py \
  scripts/prepare_swt276_official_data.py \
  scripts/prepare_swt_images.py
/root/miniconda3/envs/icore/bin/python -m unittest \
  tests.test_prompt_compaction \
  tests.test_strict_icore_selection \
  tests.test_llm_client \
  tests.test_official_generation_runtime
docker info >/dev/null
git status --short
```

实验提交应使用固定 commit。`git status` 中不应存在未提交的源码修改；本机 `.secrets/`、`data/official/`、`swe_repos/` 和 `results/` 被忽略是正常的。

运行前记录版本，后续所有续跑都必须使用同一 commit：

```bash
git rev-parse HEAD | tee /root/Baxxhy/BugReproduce/brt6/FROZEN_COMMIT.txt
```

## 12. 可选：先跑三例完整流程

三例脚本使用相同算法、模型、提示词和正式评测，只把并发降为 3：

```bash
cd /root/Baxxhy/BugReproduce/brt6
export BRT_API_POOL_FILE=/root/Baxxhy/BugReproduce/brt6/.secrets/api_pool_xiaojing_deepseekv4flash.json
bash scripts/run_xiaojing_deepseekv4flash_smoke3.sh
```

运行结束后查看脚本打印的目录，以及：

```bash
cat <RUN_DIR>/progress.log
jq . <RUN_DIR>/evaluation/official_f2p/metrics.json
```

三例结果只用于检查流程，不作为全量指标。

## 13. 启动 276 条完整实验

显式设置 `RUN_DIR`，这样网络中断或进程退出后可以在同一目录续跑：

```bash
cd /root/Baxxhy/BugReproduce/brt6
export RUN_NAME=trait_full_$(date +%Y%m%d_%H%M%S)
export RUN_DIR=/root/Baxxhy/BugReproduce/brt6/results/runs/$RUN_NAME
tmux new-session -d -s "$RUN_NAME" \
  "bash -lc 'cd /root/Baxxhy/BugReproduce/brt6 && RUN_DIR=$RUN_DIR BRT_API_POOL_FILE=/root/Baxxhy/BugReproduce/brt6/.secrets/api_pool_xiaojing_deepseekv4flash.json bash scripts/run_trait_xiaojing_deepseekv4flash_full.sh'"
echo "$RUN_NAME"
echo "$RUN_DIR"
```

查看当前阶段：

```bash
cat "$RUN_DIR/progress.log"
tail -f "$RUN_DIR/logs/01_design1.log"
```

不同阶段对应日志：

```text
logs/01_design1.log
logs/02_design2.log
logs/03_strict_icore.log
logs/04_official_evaluation.log
```

进入 tmux：

```bash
tmux attach -t "$RUN_NAME"
```

离开但不停止任务：按 `Ctrl-b`，松开后按 `d`。

## 14. 中断后续跑

不要删除原目录。使用第一次保存的完全相同 `RUN_DIR` 再执行：

```bash
cd /root/Baxxhy/BugReproduce/brt6
export RUN_DIR=/root/Baxxhy/BugReproduce/brt6/results/runs/<原运行目录名>
export RUN_NAME=$(basename "$RUN_DIR")_resume
tmux new-session -d -s "$RUN_NAME" \
  "bash -lc 'cd /root/Baxxhy/BugReproduce/brt6 && RUN_DIR=$RUN_DIR BRT_API_POOL_FILE=/root/Baxxhy/BugReproduce/brt6/.secrets/api_pool_xiaojing_deepseekv4flash.json bash scripts/run_trait_xiaojing_deepseekv4flash_full.sh'"
```

脚本使用四个阶段标记：

```text
design1.done
design2.done
selection.done
evaluation.done
```

已经完成的阶段会跳过。组件 1 和组件 2内部也使用 `--resume`。组件 1 会自动进行最多三轮补跑，只重新请求缺失或无效 JSON 的实例。

同一个 `RUN_DIR` 同一时间只能由一个进程使用。同一批实例也不要同时运行两个官方评测，以免竞争持久容器。

### 安全停止与进程回收

全量入口会为每个阶段建立独立进程组。收到 `TERM`、`INT` 或 `HUP` 后，它会先终止当前阶段的完整子进程树，再调用 `wait` 回收直接子进程。因此应优先停止启动脚本或对应 tmux 会话，不要直接终止 Docker 守护进程：

```bash
tmux kill-session -t "$SESSION"
```

测试命令和 Docker CLI 命令超时后也会先发送 `TERM`，短暂等待后才发送 `KILL`，并在返回前完成 `communicate()/wait()`。不要使用 `kill -9` 结束整条流水线或 `dockerd`；强制杀死父进程会让已经退出的孙进程交给 PID 1，若服务器的 PID 1 不负责回收，就会长期显示为 `<defunct>`。

全量入口还会在启动前检查僵尸进程和运行中的 `exec.eval.*` 容器。默认僵尸数超过 100 或存在遗留运行容器时拒绝启动。该门禁用于防止在未真正完成宿主机重启、或旧评测仍占用 Docker 时叠加新任务；不要通过提高阈值绕过宿主机故障。

## 15. 验收最终结果

实验结束后：

```bash
test -f "$RUN_DIR/completed.done"
jq . "$RUN_DIR/strict_icore/completed.json"
jq '{total_instances,f2p_success,f2p_fail,f2p_at_1_percent,by_status}' \
  "$RUN_DIR/evaluation/official_f2p/metrics.json"
jq '{resolved_ids,unresolved_ids,error_ids}' \
  "$RUN_DIR/evaluation/official_f2p/official_report.json"
```

必须核对：

- 正式分母为 276；
- `resolved_ids`、`unresolved_ids` 和 `error_ids` 覆盖全部实例且互不重叠；
- `error_ids` 为空；
- 缺少最终 BRT 的实例仍计入失败，不能从分母删除；
- 最终 F2P 为 `f2p_success / 276`。

保留以下文件用于论文复核：

```text
run_config.json
strict_icore/manifest.json
strict_icore/predictions_frozen.json
strict_icore/completed.json
evaluation/official_f2p/official_predictions.json
evaluation/official_f2p/official_run_manifest.json
evaluation/official_f2p/official_harness.log
evaluation/official_f2p/official_report.json
evaluation/official_f2p/metrics.json
```

## 16. 生成与评测隔离

组件 1、组件 2、严格验证和排序只能读取：

```text
data/issues/swt276_issues.json
retrieval_results/code/code_retrieval_results_gpt.json
retrieval_results/test/icore/gpt/related_tests.json
buggy repository 和 buggy execution
```

以下内容只能由最后的官方评测进程读取：

```text
data/official/swt276_official_eval.json
真实 patch
test_patch
fixed-side execution
官方 resolved 标签
```

全量入口在 `strict_icore/predictions_frozen.json` 生成后才启动官方评测。不要把官方评测日志复制进生成目录，也不要根据 fixed-side 结果重新生成同一批候选后将其报告为盲测结果。

## 17. 发布者提交前检查

维护者在提交到远程仓库前执行：

```bash
cd /root/Baxxhy/BugReproduce/brt6
git check-ignore .secrets/api_pool_xiaojing_deepseekv4flash.json
git check-ignore data/official/swt276_official_eval.json
/root/miniconda3/envs/icore/bin/python scripts/check_repository_secrets.py .
bash -n scripts/run_trait_xiaojing_deepseekv4flash_full.sh
/root/miniconda3/envs/icore/bin/python -m unittest \
  tests.test_prompt_compaction \
  tests.test_strict_icore_selection \
  tests.test_llm_client \
  tests.test_official_generation_runtime \
  tests.test_process_reaping
git diff --cached --check
git status --short
```

不要提交：

```text
.secrets/
data/official/
results/
swe_repos/
.runtime/
.bootstrap/
Docker 数据目录
```

空服务器会按照本文重新生成这些内容。
