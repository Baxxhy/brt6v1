# TRAIT 最新完整实验运行文档（2026-09-18）

本文说明如何在 `/root` 下准备环境、运行 TRAIT 完整方法、运行去除组件 1 的消融实验，并完成 SWT-Bench Lite 276 个实例的官方 F2P 评测。

本文对应的算法版本为：

```text
lossless_issue_target_fallback_v1
```

完整方法的执行过程如下：

```text
原始 Issue + 仓库上下文 + 检索测试
→ 组件 1：恢复结构化 Reproduction Target
→ 原始 Issue 与 Reproduction Target 同时传入组件 2
→ 组件 2：分别适配 Top-3 检索测试并执行 Semantic Delta 反馈
→ 严格语义验证
→ 若三个目标引导分支均未产生合格 BRT，执行一次直接适配兜底
→ 保留生成阶段冻结的目标路径或兜底路径
→ 路径内使用确定性 iCoRe 排序
→ 每个实例输出一个最终 BRT
→ 官方 SWT-Docker F2P 评测
```

生成、严格验证和排序只读取 buggy 版本。真实 patch、fixed 版本和官方 F2P 标签只能由最后的评测阶段读取。

## 1. 目录与资源要求

固定项目目录：

```text
/root/Baxxhy/BugReproduce/brt6
```

建议服务器配置：

- Ubuntu 22.04 或 24.04；
- Linux x86-64；
- 16 核或更多 CPU；
- 64 GiB 或更多内存；
- 至少 300 GiB 可用 SSD；
- 无需 GPU；
- 能访问 GitHub、Docker、Conda、PyPI、Hugging Face 和模型 API；
- 以 `root` 运行，或具有 `sudo` 和 Docker 权限。

实验使用以下主要路径：

```text
/root/Baxxhy/BugReproduce/brt6
/root/Baxxhy/BugReproduce/swe_repos
/root/lby-docker
/var/run/docker.sock
```

Conda 环境的实际位置由初始化脚本探测并写入
`.bootstrap/use_fresh_swt_server.sh`，正式入口不再依赖固定的 Conda
安装目录。

## 2. 克隆最新代码

```bash
mkdir -p /root/Baxxhy/BugReproduce
cd /root/Baxxhy/BugReproduce
git clone https://github.com/Baxxhy/brt6v1.git brt6
cd brt6
git status --short
git log -1 --oneline
```

`git status --short` 在新服务器上应为空。

## 3. 准备系统、Conda、Docker、仓库和评测环境

### 3.1 在空服务器安装 Conda 和 Docker

如果服务器没有 Conda：

```bash
cd /root
curl -fsSLo /tmp/miniconda.sh \
  https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash /tmp/miniconda.sh -b -p /root/miniconda3
export PATH=/root/miniconda3/bin:$PATH
conda --version
```

如果服务器没有 Docker：

```bash
apt-get update
apt-get install -y docker.io
mkdir -p /root/lby-docker /etc/docker
cat >/etc/docker/daemon.json <<'EOF'
{
  "data-root": "/root/lby-docker"
}
EOF
systemctl enable --now docker
systemctl restart docker
export DOCKER_HOST=unix:///var/run/docker.sock
docker info --format 'driver={{.Driver}} root={{.DockerRootDir}}'
```

如果服务器没有 `systemd`，以等价方式启动 `dockerd`，但必须确保
`docker info` 成功且 data-root 位于 `/root/lby-docker`。

### 3.2 初始化 TRAIT 实验环境

运行项目提供的空服务器初始化脚本：

```bash
cd /root/Baxxhy/BugReproduce/brt6
bash scripts/bootstrap_fresh_swt_server.sh
```

如果系统依赖已经安装：

```bash
bash scripts/bootstrap_fresh_swt_server.sh \
  --skip-system-packages \
  --prewarm-workers 8
```

初始化脚本会创建 `icore` 控制器环境和 `swtbench` 官方评测环境，
安装 `requirements-reproduce.txt`，准备 12 个项目仓库，并预热 52 个
SWT 依赖模板。完成后加载脚本实际探测到的环境路径：

```bash
source /root/Baxxhy/BugReproduce/brt6/.bootstrap/use_fresh_swt_server.sh
export DOCKER_HOST=unix:///var/run/docker.sock
"$PYTHON_BIN" -c 'import datasets, docker, requests, unidiff; print("controller=ready")'
"$SWTBENCH_PYTHON" -c 'import docker, unidiff; print("official_harness=ready")'
```

如果已存在可用的 `/root/miniconda3/envs/icore`，也可以显式复用它：

```bash
conda create -n icore -y python=3.12 pip
/root/miniconda3/envs/icore/bin/python -m pip install -r requirements-reproduce.txt
bash scripts/bootstrap_fresh_swt_server.sh \
  --controller-python /root/miniconda3/envs/icore/bin/python \
  --prewarm-workers 8
```

## 4. 准备官方评测数据

生成阶段使用的 issue 文件不包含 gold patch：

```text
data/issues/swt276_issues.json
```

官方评测数据在新服务器上单独生成：

```bash
cd /root/Baxxhy/BugReproduce/brt6
source .bootstrap/use_fresh_swt_server.sh
"$PYTHON_BIN" scripts/prepare_swt276_official_data.py
"$PYTHON_BIN" scripts/prepare_swt276_official_data.py --check
```

预期生成：

```text
data/official/swt276_official_eval.json
```

该文件只能在最终输出冻结后由官方评测进程读取，不能加入生成提示词或排序输入。

## 5. 配置模型 API

API key 写入被 Git 忽略的本机文件：

```bash
cd /root/Baxxhy/BugReproduce/brt6
mkdir -p .secrets
source .bootstrap/use_fresh_swt_server.sh
"$PYTHON_BIN" scripts/configure_api_keys.py \
  --output .secrets/api_pool.json \
  --provider deepseek \
  --base-url https://api.open.xiaojingai.com/v1 \
  --model deepseek-v4-flash
chmod 600 .secrets/api_pool.json
```

配置文件支持多个 endpoint。请求失败、限流或网络异常时，API 池会轮换到下一个可用 endpoint。

测试真实请求：

```bash
export BRT_API_POOL_FILE=/root/Baxxhy/BugReproduce/brt6/.secrets/api_pool.json
export BRT_ALLOWED_API_HOST=api.open.xiaojingai.com
export BRT_DISABLE_THINKING=1
"$PYTHON_BIN" scripts/check_configured_api.py \
  --provider deepseek \
  --model deepseek-v4-flash \
  --stream
```

不要把 `.secrets/`、终端中打印的 key 或包含 key 的日志提交到 Git。

## 6. 检查 Docker

```bash
export DOCKER_HOST=unix:///var/run/docker.sock
docker info --format 'driver={{.Driver}} root={{.DockerRootDir}}'
docker ps -a --format '{{.Names}}\t{{.Status}}\t{{.Image}}' | head
df -h /root/lby-docker
```

预期 Docker data-root 为 `/root/lby-docker`。正式实验开始前不应存在其他正在运行的 `exec.eval.*` 容器。若服务器已经使用可用的自定义 Docker socket，也可以导出该 socket；正式入口会尊重已有的 `DOCKER_HOST`。

## 7. 运行前离线检查

下面的命令不会调用模型或官方 Docker 评测：

```bash
cd /root/Baxxhy/BugReproduce
source brt6/.bootstrap/use_fresh_swt_server.sh
"$PYTHON_BIN" -m pytest -q \
  brt6/tests/test_behavior_target_ablation.py \
  brt6/tests/test_feedback_ablations.py \
  brt6/tests/test_strict_icore_selection.py \
  brt6/tests/test_official_generation_runtime.py

cd /root/Baxxhy/BugReproduce/brt6
source .bootstrap/use_fresh_swt_server.sh
export DOCKER_HOST=unix:///var/run/docker.sock
bash -n scripts/run_trait_xiaojing_deepseekv4flash_full.sh
"$PYTHON_BIN" -m py_compile \
  execution/feedback.py \
  generation/generator.py \
  mutation/seed_mutator.py \
  scripts/run_generation_strict_icore.py
```

## 8. 先运行 3 例冒烟实验

```bash
cd /root/Baxxhy/BugReproduce/brt6
export BRT_API_POOL_FILE=/root/Baxxhy/BugReproduce/brt6/.secrets/api_pool.json
source .bootstrap/use_fresh_swt_server.sh
export DOCKER_HOST=unix:///var/run/docker.sock
bash scripts/run_xiaojing_deepseekv4flash_smoke3.sh
```

至少检查：

- 组件 1 能生成 `behavior_target.json`；
- 组件 2 能生成三个独立 seed 目录；
- `summary.json` 中包含 `algorithm_revision` 对应的新签名；
- `selection_route` 为 `target_primary` 或 `direct_fallback_after_target_exhaustion`；
- 严格排序阶段能够保留生成阶段冻结的 route；
- 官方评测没有基础设施错误。

## 9. 运行 276 例完整方法

当前正式的一键入口为：

```bash
cd /root/Baxxhy/BugReproduce/brt6
export BRT_API_POOL_FILE=/root/Baxxhy/BugReproduce/brt6/.secrets/api_pool.json
source .bootstrap/use_fresh_swt_server.sh
export DOCKER_HOST=unix:///var/run/docker.sock
export RUN_TIMESTAMP=$(date +%Y%m%d_%H%M%S)
bash scripts/run_trait_xiaojing_deepseekv4flash_full.sh
```

固定配置：

| 配置 | 值 |
|---|---:|
| 实例数 | 276 |
| 模型 | `deepseek-v4-flash` |
| 推理模式 | 关闭 |
| 温度 | 0.1 |
| 初始最大输出 | 4096 tokens |
| 截断重试最大输出 | 8192 tokens |
| 组件 1 并发 | 20 |
| 组件 2 并发 | 20 |
| 排序并发 | 20 |
| 官方评测并发 | 20 |
| API endpoint 超时 | 600 秒 |
| 单实例 Docker 超时 | 7200 秒 |
| Semantic Delta 最大轮数 | 5 |
| Coverage | 关闭，仅计算 F2P |

运行目录默认为：

```text
results/runs/trait_xiaojing_deepseekv4flash_full_<timestamp>/
```

完整入口依次执行：

```text
stage 1/4  Reproduction Target Recovery
stage 2/4  Individual Test Adaptation
stage 3/4  Strict filtering + route-preserving iCoRe ranking
stage 4/4  Official SWT-Docker F2P evaluation
```

## 10. 中断后继续运行

必须复用原来的 `RUN_DIR`：

```bash
cd /root/Baxxhy/BugReproduce/brt6
export BRT_API_POOL_FILE=/root/Baxxhy/BugReproduce/brt6/.secrets/api_pool.json
source .bootstrap/use_fresh_swt_server.sh
export DOCKER_HOST=unix:///var/run/docker.sock
export RUN_DIR=/root/Baxxhy/BugReproduce/brt6/results/runs/<原运行目录名>
bash scripts/run_trait_xiaojing_deepseekv4flash_full.sh
```

脚本会检查每个阶段的完成标记和实际 JSON 数量。标记与真实文件不一致时，自动删除失效的下游标记并补跑缺失内容。

新的 `algorithm_revision` 已写入 ablation signature，旧版本的 `summary.json` 不会被 `--resume` 错误复用。

## 11. 查看进度和日志

```bash
RUN_DIR=/root/Baxxhy/BugReproduce/brt6/results/runs/<运行目录名>

tail -f "$RUN_DIR/progress.log"
tail -f "$RUN_DIR/logs/01_design1.log"
tail -f "$RUN_DIR/logs/02_design2.log"
tail -f "$RUN_DIR/logs/03_strict_icore.log"
tail -f "$RUN_DIR/logs/04_official_evaluation.log"
```

统计组件 1 目标数量：

```bash
find "$RUN_DIR/design1_reproduction_target" -mindepth 2 \
  -name behavior_target.json | wc -l
```

统计组件 2 顶层结果数量：

```bash
find "$RUN_DIR/design2_individual_adaptation" -mindepth 2 \
  -maxdepth 2 -name summary.json | wc -l
```

统计兜底使用数量：

```bash
jq -r 'select(.direct_fallback_used == true) | .instance_id' \
  "$RUN_DIR"/design2_individual_adaptation/*/summary.json | wc -l
```

## 12. 查看最终 F2P 结果

```bash
RUN_DIR=/root/Baxxhy/BugReproduce/brt6/results/runs/<运行目录名>
jq . "$RUN_DIR/evaluation/official_f2p/metrics.json"
```

必须核对：

- `total_instances` 为 276；
- `error_ids` 为空；
- 缺少最终 BRT 的实例仍保留在 276 的分母中；
- `resolved_ids` 是严格 F2P；
- `unresolved_ids`、`resolved_ids` 和 `error_ids` 覆盖全部实例。

需要保留的核心复核文件：

```text
run_config.json
design1_completion.json
design2_individual_adaptation/summary.json
strict_icore/manifest.json
strict_icore/predictions_frozen.json
strict_icore/completed.json
evaluation/official_f2p/official_predictions.json
evaluation/official_f2p/official_report.json
evaluation/official_f2p/metrics.json
```

## 13. 运行去除组件 1 的消融实验

消融实验不执行 IssueRewrite，不读取 BehaviorTarget 缓存；其余候选数量、组件 2、严格校验、排序和官方评测保持一致。

```bash
cd /root/Baxxhy/BugReproduce/brt6
export BRT_API_POOL_FILE=/root/Baxxhy/BugReproduce/brt6/.secrets/api_pool.json
source .bootstrap/use_fresh_swt_server.sh
export DOCKER_HOST=unix:///var/run/docker.sock
export RUN_DIR=/root/Baxxhy/BugReproduce/brt6/results/runs/ablation_wo_behavior_target_$(date +%Y%m%d_%H%M%S)
export ISSUE_WORKERS=20
export GENERATION_WORKERS=20
export EVALUATION_WORKERS=20
export GENERATION_TIMEOUT=7200
export FORMAL_EVAL_TIMEOUT=7200
bash scripts/run_p0_simple_llm_selector_full.sh \
  --dataset swt \
  --model deepseek \
  --behavior-target off
```

完整方法与消融必须使用：

- 相同数据集；
- 相同检索结果；
- 相同模型和温度；
- 相同 Top-3 候选预算；
- 相同最大反馈轮数；
- 相同并发和超时；
- 相同官方评测脚本。

不要把完整方法生成的 BehaviorTarget 缓存传给 `--behavior-target off`，启动脚本会拒绝这种无效消融配置。

## 14. 只重新进行官方评测

若候选已经冻结，只需重新评测：

```bash
cd /root/Baxxhy/BugReproduce/brt6
RUN_DIR=/root/Baxxhy/BugReproduce/brt6/results/runs/<运行目录名>

source .bootstrap/use_fresh_swt_server.sh
export DOCKER_HOST=unix:///var/run/docker.sock
"$SWTBENCH_PYTHON" \
  scripts/run_official_eval_after_generation.py \
  --dataset swt \
  --outputs-dir "$RUN_DIR/strict_icore/generation" \
  --dataset-file data/official/swt276_official_eval.json \
  --official-dataset-name data/official/swt276_official_eval.json \
  --evaluation-dir "$RUN_DIR/evaluation/official_f2p" \
  --max-workers 20 \
  --timeout 7200 \
  --run-id "$(basename "$RUN_DIR")" \
  --model-name trait \
  --compute-coverage false \
  --official-python "$SWTBENCH_PYTHON" \
  --swtbench-root evaluation/vendor/swtbench
```

## 15. 常见问题

### 15.1 API 请求慢或限流

先验证配置：

```bash
source .bootstrap/use_fresh_swt_server.sh
"$PYTHON_BIN" scripts/check_configured_api.py \
  --provider deepseek --model deepseek-v4-flash --stream
```

不要同时启动多个全量生成任务争抢同一个 API 池。相同 `RUN_DIR` 下重新启动会补跑缺失实例。

### 15.2 大量僵尸进程

僵尸进程只能由其父进程回收。正式入口已经使用进程组终止和 `wait` 回收子进程。如果服务器长期累积不可回收僵尸，先停止实验并重启外层服务器或容器，再复用原 `RUN_DIR` 继续。

### 15.3 最终排序没有采用 direct fallback

检查：

```bash
jq '{selection_route,direct_fallback_attempted,direct_fallback_used,direct_fallback_status}' \
  "$RUN_DIR/design2_individual_adaptation/<instance_id>/summary.json"
jq '.generation_routes' "$RUN_DIR/strict_icore/manifest.json"
```

`strict_icore/manifest.json` 必须记录 `direct_fallback_after_target_exhaustion` 数量。当前排序脚本会沿用生成阶段冻结的 route，不再退回目标路径的旧 seed。

### 15.4 为什么不能保证提高固定百分点

生成时看不到真实修复，因此任何不读取 fixed 的算法都不能预先保证 F2P 提高固定数值。实验必须先冻结输出，再运行官方评测。不得使用 official 标签选择候选或调整同一批实例后继续声称盲测。

## 16. 提交前检查

```bash
cd /root/Baxxhy/BugReproduce/brt6
bash -n scripts/run_trait_xiaojing_deepseekv4flash_full.sh
source .bootstrap/use_fresh_swt_server.sh
"$PYTHON_BIN" -m pytest -q \
  tests/test_behavior_target_ablation.py \
  tests/test_feedback_ablations.py \
  tests/test_strict_icore_selection.py
git diff --check
git status --short
```

禁止提交：

```text
.secrets/
data/official/
results/
swe_repos/
.runtime/
.bootstrap/
Docker 数据目录
API key 和包含 key 的日志
```
