# 2026-09-23 TRAIT 运行文档

## 1. 固定版本

本文档所在 Git 提交固定了当前端到端流程：

```text
Issue + repository context
→ BehaviorTarget
→ 对 iCoRe Top-3 测试分别适配
→ Semantic Delta + buggy 执行反馈
→ 严格语义校验
→ strict-iCoRe 选择
→ 每个实例一个 BRT
→ 官方 Docker F2P 评测
```

2026-09-23 的 C1 最小修订也包含在本提交中：如果 issue 中的外部工具在当前仓库源码和已有测试中没有依据，BehaviorTarget 不再把它当作必需依赖、目标 API 或硬触发条件，而是优先使用仓库原生路径复现同一行为。

已有结果：

- GPT-5.4-mini 单次全量冻结结果：156/276（56.52%）；
- 最小修订的 15 例开发验证：4/15；
- 4 个新 F2P 与前述 156 个无重合，历史结果并集为 160/276（57.97%）。

**160/276 是两批冻结结果的实例级并集，不是单次端到端 Top-1 分数。**

## 2. 运行前检查

本文档假定 Conda、Docker、官方评测环境、buggy 仓库和 API 池已经配好。

```bash
cd /root/Baxxhy/BugReproduce/brt6
source .bootstrap/use_fresh_swt_server.sh

git log -1 --oneline
git status --short
docker version >/dev/null
"$PYTHON_BIN" scripts/configure_api_keys.py --check --provider gpt
"$PYTHON_BIN" scripts/configure_api_keys.py --check --provider deepseek
```

默认 API 池是 `.secrets/api_pool.json`。使用另一个已配置的 API 池：

```bash
export BRT_API_POOL_FILE=/root/private/api_pool.json
```

## 3. 先跑 3 例预检

同时裁剪 issue 和 gold，保证生成与评测的实例集一致：

```bash
"$PYTHON_BIN" - <<'PY'
import json
from pathlib import Path

def rows(path):
    data = json.loads(Path(path).read_text())
    return data if isinstance(data, list) else list(data.values())

selected = rows("data/issues/swt276_issues.json")[:3]
ids = {str(row["instance_id"]) for row in selected}
selected_gold = [
    row for row in rows("data/official/swe_bench_lite_test.json")
    if str(row.get("instance_id")) in ids
]
assert len(selected) == len(selected_gold) == 3
Path("/root/trait-smoke3-issues.json").write_text(
    json.dumps(selected, ensure_ascii=False, indent=2) + "\n"
)
Path("/root/trait-smoke3-gold.json").write_text(
    json.dumps(selected_gold, ensure_ascii=False, indent=2) + "\n"
)
PY
```

固定运行参数：

```bash
export BRT_API_POOL_FILE="$PWD/.secrets/api_pool.json"
export BRT_DISABLE_THINKING=1
export BRT_LLM_STREAM=1
export BRT3_LLM_REQUEST_TIMEOUT=1200
export BRT_LLM_TRUNCATION_MAX_TOKENS=8192
export BRT_LLM_POOL_ROTATION_ATTEMPTS=10
export BRT_OFFICIAL_DOCKER_STARTUP_TIMEOUT=7200
```

启动 GPT-5.4-mini 预检：

```bash
bash scripts/launch_semantic_delta_full.sh \
  --dataset swt \
  --model gpt \
  --model-id gpt-5.4-mini \
  --instances /root/trait-smoke3-issues.json \
  --gold-dataset /root/trait-smoke3-gold.json \
  --max-rounds 5 \
  --issue-workers 3 \
  --generation-workers 3 \
  --evaluation-workers 3 \
  --generation-timeout 7200 \
  --evaluation-timeout 7200 \
  --run-name trait_gpt54mini_smoke3_YYYYMMDD \
  --f2p-only \
  --allow-external-model-data
```

## 4. 跑 GPT-5.4-mini 全量 SWT

预检的 IssueRewrite、Top-3 适配、strict-iCoRe 和官方评测都完成后，使用新 run name 启动全量：

```bash
bash scripts/launch_semantic_delta_full.sh \
  --dataset swt \
  --model gpt \
  --model-id gpt-5.4-mini \
  --max-rounds 5 \
  --issue-workers 20 \
  --generation-workers 20 \
  --evaluation-workers 20 \
  --generation-timeout 7200 \
  --evaluation-timeout 7200 \
  --run-name trait_gpt54mini_full_YYYYMMDD \
  --f2p-only \
  --allow-external-model-data
```

`--allow-external-model-data` 表示允许将 issue、检索代码和测试片段发送到已配置的模型网关。

## 5. 查看进度和结果

```bash
tmux -L brt6-runs ls
tail -f results/runs/trait_gpt54mini_full_YYYYMMDD.launcher.log
```

主要结果文件：

```text
results/runs/<run-name>/generation/summary.json
results/runs/<run-name>/strict_icore/predictions_frozen.json
results/runs/<run-name>/evaluation/formal_f2p/metrics.json
results/runs/<run-name>/evaluation/formal_f2p/official_report.json
results/runs/<run-name>/completion.json
results/runs/<run-name>/api_cost_summary.json
results/runs/<run-name>/api_cost_summary.md
```

快速查分：

```bash
python - <<'PY'
import json
from pathlib import Path
p = Path("results/runs/<run-name>/evaluation/formal_f2p/metrics.json")
d = json.loads(p.read_text())
print(f"F2P: {d['f2p_success']}/{d['total_instances']}")
print(f"F2P@1: {d['f2p_at_1_percent']:.2f}%")
PY
```

## 6. 切换模型

### DeepSeek-V4-Flash

保持其他参数不变，只改 provider、model ID 和 run name：

```bash
bash scripts/launch_semantic_delta_full.sh \
  --dataset swt \
  --model deepseek \
  --model-id deepseek-v4-flash \
  --max-rounds 5 \
  --issue-workers 20 \
  --generation-workers 20 \
  --evaluation-workers 20 \
  --generation-timeout 7200 \
  --evaluation-timeout 7200 \
  --run-name trait_deepseekv4flash_full_YYYYMMDD \
  --f2p-only \
  --allow-external-model-data
```

### 其他 OpenAI-compatible 模型

API 池中每条记录包含 `name`、`provider`、`api_key`、`base_url` 和 `model`。`provider` 目前使用 `gpt` 或 `deepseek` 隔离 key 池；`--model-id` 指定真正的请求模型。

```bash
bash scripts/launch_semantic_delta_full.sh \
  --dataset swt \
  --model gpt \
  --model-id gpt-5-mini \
  --issue-workers 20 \
  --generation-workers 20 \
  --evaluation-workers 20 \
  --run-name trait_gpt5mini_full_YYYYMMDD \
  --f2p-only \
  --allow-external-model-data
```

比较模型时，必须固定 Git commit、数据集、Top-3 检索结果、轮数、提示词和评测环境。

## 7. 切换数据集

### TDD-Bench

```bash
bash scripts/launch_semantic_delta_full.sh \
  --dataset tdd \
  --model gpt \
  --model-id gpt-5.4-mini \
  --max-rounds 5 \
  --issue-workers 20 \
  --generation-workers 20 \
  --evaluation-workers 20 \
  --generation-timeout 7200 \
  --evaluation-timeout 7200 \
  --run-name trait_gpt54mini_tdd_full_YYYYMMDD \
  --f2p-only \
  --allow-external-model-data
```

### SWT 子集或新 SWT-compatible 数据

```bash
export CODE_RETRIEVAL=/root/my_dataset/code_retrieval.json
export TEST_RETRIEVAL=/root/my_dataset/related_tests.json
export REPO_ROOT=/root/my_dataset/repositories

bash scripts/launch_semantic_delta_full.sh \
  --dataset swt \
  --model gpt \
  --model-id gpt-5.4-mini \
  --instances /root/my_dataset/issues.json \
  --gold-dataset /root/my_dataset/official_eval.json \
  --official-python /root/miniconda3/envs/swtbench/bin/python \
  --issue-workers 20 \
  --generation-workers 20 \
  --evaluation-workers 20 \
  --run-name trait_custom_swt_YYYYMMDD \
  --f2p-only \
  --allow-external-model-data
```

换数据集时必须同时对齐 issue、gold、buggy 仓库及 commit、code retrieval、Top-3 test retrieval 和官方 Docker harness。`instance_id` 必须在这些输入中一致。新数据集先跑 3–5 例，再跑全量。

## 8. 停止、续跑和 API 错误

停止指定任务：

```bash
tmux -L brt6-runs kill-session -t <session-name>
```

不要删除已有 run 目录。流水线会保存实例 checkpoint；运行目录中有 `resume_existing.sh` 时，优先使用它续跑。

HTTP 524/525/556 通常是共享网关或上游超时。当前客户端会轮换同 provider 的 key，并由 `BRT_LLM_POOL_ROTATION_ATTEMPTS` 限制轮换次数。API 错误不应计为算法 F2F。

## 9. 结果报告约束

每次正式实验保存 Git commit、模型、provider、轮数、并发度、数据与检索输入、生成完成数、基础设施错误、F2P 分子/分母和 API 成本。

主实验不得在生成或选择时读取 fixed 日志、gold patch 或官方 F2P/F2F 标签。这些信息只能在输出冻结后由独立评测进程使用。单次 Top-1 和多次运行并集必须分开报告。
