The current test has setup/import/fixture/class/collection/syntax issues. Only fix the executable context; do not change the defect trigger target or any Oracle protocol (including exceptions, warnings, logging, state, return values, and framework matchers).

You must return the complete Python test file, which will be saved as `test_brt_<instance_id>.py` in the same directory.

Do not return method fragments; do not rely on insertion into an existing class/file.

Prefer to remove third-party imports that have no contextual basis and do not exist in the environment; do not suggest installing new dependencies.

You must first restore the original class's class-level attributes from `HostContext.setup_context`; do not fabricate checker/model/helper names that do not exist.

When the test function name, class wrapper, or runner does not match, ensure the real test name in the returned code matches the name collectable by the HostContext runner.

If the HostContext runner is SymPy bin/test, do not use pytest fixtures; if it is Django runtests, do not manually configure a global settings that conflicts with the project's test app.

If HostContext already provides usable project test models, fixtures, or helpers, you must reuse them; do not redeclare models with the same name.

`HostContext.model_context` contains the real definitions of these imported models in the buggy repository; field names, required fields, relationship types, and app_label must follow it exactly; do not guess the repository model structure based on the Issue example.

Do not add module names or temporary app_labels that do not exist in the repository to `INSTALLED_APPS`. Only apps explicitly present in HostContext or repository files may enter the Django configuration.

In Django, `Model.field_name` is typically a `DeferredAttribute` descriptor, not a `Field` instance; when you need to call a field method, use `Model._meta.get_field("field_name")`. If a standalone `Field` call to `check()` fails due to missing name/model metadata, first call `set_attributes_from_name()` to fill in the field name, or obtain the field via `_meta.get_field()` on an existing test model; do not fall back to unrelated `clean()`/`save()` paths.

When an Issue requires new logging but the buggy source code does not yet have a logger, the setup fix must preserve the `assertLogs`/`caplog` observation method; do not patch a non-existent logger or `_logger` attribute to create an environment error.

If the path generated by `MigrationWriter`, `deconstruct`, `pickle`, `serializer`, or other serialization processes contains `<locals>`, it means the custom class or function being serialized is still defined inside the test method. You must move these definitions to the top level of the Python file module so that their `__module__` and `__qualname__` are importable; merely modifying comments, renaming local classes, or claiming "module-level" inside the method is not a fix.

If the test uses `exec()` to execute `inspectdb` or other dynamically generated Django model code, you must provide a module/app context that actually exists in the repository and is registrable for the execution namespace, or only perform compile/structure checks on the generated text aligned with the Issue; do not let the dynamic model fail before the target behavior due to missing `app_label`/`INSTALLED_APPS`.

If testing a management command's parser/help behavior, prefer to directly instantiate an existing or test-defined `Command` and call `create_parser()`/`print_help()`/`run_from_argv()`. Temporarily modifying `DJANGO_SETTINGS_MODULE`, `sys.path`, and creating an app directory after the Django test process has started will not reliably refresh the management command registration cache; do not treat `fetch_command`'s `KeyError` as a defect result.

If `NoReverseMatch` occurs after the test registers its own admin/model/URL, and the Issue is not specifically about `NoReverseMatch`, you must fix the real admin registration and URL name, or directly verify the URL semantics described in the Issue from the actual response/link; do not treat the test's self-created non-existent URL as a defect.

If SQLite reports a SQL syntax error during table creation due to a PostgreSQL-specific field (e.g., `[]` type for `ArrayField`), you must avoid having that model participate in SQLite test database table creation. Prefer to reuse the repository's existing PostgreSQL test context, or set the temporary model used only for form/field/formset construction to `Meta.managed = False` and avoid database reads/writes, reproducing input parsing and state retention behavior at the non-table level. Simply adding `skipUnlessDBFeature` is ineffective because the test database is created before the test method executes. Do not treat backend incompatibility as an Issue failure.

Behavior target: {behavior_json}
HostContext: {host_context_json}
Current test: {candidate_code}
Execution log: {execution_log}
Verifier feedback: {verifier_feedback}

Strictly follow Semantic Delta: preserve what is to be preserved; this round only handles the single change corresponding to setup; avoid what is to be avoided; leave other differences for subsequent rounds; do not modify Trigger or Oracle incidentally.

You must prioritize completing the `next_action` in the Verifier feedback. If the feedback points to a specific import, fixture, class, nodeid, or setup issue, the returned code must substantially modify the corresponding location; do not return it unchanged.
